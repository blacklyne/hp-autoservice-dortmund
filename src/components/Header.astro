---
import { site, nav } from "~/data/site";
import Icon from "./Icon.astro";

const path = Astro.url.pathname.replace(/\/$/, "") || "/";
---

<header class="site-header" id="site-header">
  <div class="container-x">
    <div class="header-inner">
      <a href="/" class="brand" aria-label={`${site.name} - Startseite`}>
        <img
          src="/logo.png"
          alt={site.name}
          class="brand-logo"
          width="56"
          height="50"
        />
        <span class="brand-text">
          <span class="brand-name">H.P. Autoservice</span>
          <span class="brand-sub">Kfz-Meisterbetrieb · Dortmund-Derne</span>
        </span>
      </a>

      <nav class="nav-desktop" aria-label="Hauptnavigation">
        {
          nav.map((item) => {
            const itemPath = item.href.replace(/\/$/, "") || "/";
            const isActive = itemPath === path;
            return (
              <a href={item.href} class={isActive ? "nav-link active" : "nav-link"}>
                {item.label}
              </a>
            );
          })
        }
      </nav>

      <a href={site.phone.href} class="phone-cta">
        <Icon name="phone" size={18} />
        <span>{site.phone.display}</span>
      </a>

      <button class="menu-toggle" aria-label="Menü öffnen" aria-expanded="false" id="menu-toggle">
        <Icon name="menu" size={26} />
      </button>
    </div>
  </div>

  <div class="mobile-menu" id="mobile-menu" hidden>
    <nav aria-label="Mobile Navigation">
      {
        nav.map((item) => {
          const itemPath = item.href.replace(/\/$/, "") || "/";
          const isActive = itemPath === path;
          return (
            <a href={item.href} class={isActive ? "mobile-link active" : "mobile-link"}>
              {item.label}
            </a>
          );
        })
      }
      <a href={site.phone.href} class="btn btn-accent mobile-cta">
        <Icon name="phone" size={18} />
        Jetzt anrufen
      </a>
    </nav>
  </div>
</header>

<style>
  .site-header {
    position: sticky;
    top: 0;
    z-index: 50;
    background-color: #0E0E0E;
    background-image:
      linear-gradient(180deg, rgba(14, 14, 14, 0.85) 0%, rgba(14, 14, 14, 0.92) 100%),
      url("https://images.unsplash.com/photo-1486006920555-c77dcf18193c?auto=format&fit=crop&w=1800&q=70");
    background-size: cover;
    background-position: center;
    backdrop-filter: blur(8px);
    -webkit-backdrop-filter: blur(8px);
    border-bottom: 1px solid var(--color-line);
    transition: box-shadow 0.2s ease;
  }
  .site-header.scrolled {
    box-shadow: 0 2px 12px rgba(0, 0, 0, 0.5);
    background-image:
      linear-gradient(180deg, rgba(14, 14, 14, 0.94) 0%, rgba(14, 14, 14, 0.97) 100%),
      url("https://images.unsplash.com/photo-1486006920555-c77dcf18193c?auto=format&fit=crop&w=1800&q=70");
  }

  .header-inner {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1.5rem;
    height: 4.75rem;
  }

  .brand {
    display: flex;
    align-items: center;
    gap: 0.7rem;
    text-decoration: none;
  }
  .brand-logo {
    display: block;
    width: auto;
    height: 2.75rem;
    object-fit: contain;
    flex-shrink: 0;
    mix-blend-mode: lighten;
  }
  .brand-text {
    display: flex;
    flex-direction: column;
    line-height: 1.1;
  }
  .brand-name {
    font-weight: 800;
    font-size: 1.05rem;
    color: #fff;
    letter-spacing: -0.01em;
  }
  .brand-sub {
    font-size: 0.7rem;
    font-weight: 700;
    color: var(--color-yellow);
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }

  .nav-desktop {
    display: none;
    align-items: center;
    gap: 0.25rem;
    margin-left: auto;
  }
  @media (min-width: 1024px) {
    .nav-desktop {
      display: flex;
    }
  }

  .nav-link {
    padding: 0.5rem 0.95rem;
    border-radius: 0.5rem;
    font-weight: 600;
    font-size: 0.95rem;
    color: rgba(255, 255, 255, 0.85);
    transition: color 0.15s ease, background 0.15s ease;
  }
  .nav-link:hover {
    color: var(--color-yellow);
    background: rgba(255, 255, 255, 0.06);
  }
  .nav-link.active {
    color: #1A1A1A;
    background: var(--color-yellow);
  }

  .phone-cta {
    display: none;
    align-items: center;
    gap: 0.5rem;
    background: var(--color-yellow);
    color: var(--color-black);
    font-weight: 700;
    font-size: 0.95rem;
    padding: 0.65rem 1.1rem;
    border-radius: 0.5rem;
    margin-left: 0.5rem;
    transition: all 0.15s ease;
    text-decoration: none;
    border: 1.5px solid var(--color-yellow);
  }
  .phone-cta:hover {
    background: var(--color-yellow-hover);
    border-color: var(--color-yellow-hover);
    color: var(--color-black);
    transform: translateY(-1px);
    box-shadow: 0 4px 12px rgba(254, 196, 32, 0.4);
  }
  @media (min-width: 768px) {
    .phone-cta {
      display: inline-flex;
    }
  }

  .menu-toggle {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 2.5rem;
    height: 2.5rem;
    border-radius: 0.5rem;
    background: transparent;
    color: #fff;
    border: 1px solid rgba(255, 255, 255, 0.25);
    cursor: pointer;
    margin-left: auto;
  }
  @media (min-width: 1024px) {
    .menu-toggle {
      display: none;
    }
  }

  .mobile-menu {
    display: none;
    border-top: 1px solid var(--color-line);
    padding: 1rem 1.25rem 1.5rem;
    background: rgba(14, 14, 14, 0.97);
    backdrop-filter: blur(12px);
  }
  .mobile-menu[hidden] {
    display: none;
  }
  .mobile-menu.is-open {
    display: block;
  }

  .mobile-menu nav {
    display: flex;
    flex-direction: column;
    gap: 0.25rem;
  }
  .mobile-link {
    display: block;
    padding: 0.85rem 1rem;
    border-radius: 0.5rem;
    font-weight: 600;
    color: #fff;
    text-decoration: none;
    border: 1px solid transparent;
  }
  .mobile-link:hover {
    background: rgba(255, 255, 255, 0.06);
    color: var(--color-yellow);
  }
  .mobile-link.active {
    background: var(--color-yellow);
    color: #1A1A1A;
  }
  .mobile-cta {
    margin-top: 0.5rem;
    width: 100%;
  }
</style>

<script>
  const header = document.getElementById("site-header");
  const toggle = document.getElementById("menu-toggle");
  const menu = document.getElementById("mobile-menu");

  if (header) {
    const onScroll = () => {
      if (window.scrollY > 8) header.classList.add("scrolled");
      else header.classList.remove("scrolled");
    };
    onScroll();
    window.addEventListener("scroll", onScroll, { passive: true });
  }

  if (toggle && menu) {
    toggle.addEventListener("click", () => {
      const open = menu.classList.toggle("is-open");
      if (open) menu.removeAttribute("hidden");
      else menu.setAttribute("hidden", "");
      toggle.setAttribute("aria-expanded", String(open));
    });
  }
</script>
