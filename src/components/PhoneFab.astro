---
import { site } from "~/data/site";
import Icon from "./Icon.astro";
---

<a href={site.phone.href} class="phone-fab" aria-label="Anrufen">
  <Icon name="phone" size={22} />
</a>

<style>
  .phone-fab {
    position: fixed;
    right: 1.1rem;
    bottom: 1.1rem;
    z-index: 40;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 3.5rem;
    height: 3.5rem;
    border-radius: 999px;
    background: var(--color-yellow);
    color: var(--color-black);
    box-shadow: 0 6px 20px rgba(254, 196, 32, 0.5), 0 2px 6px rgba(0, 0, 0, 0.15);
    transition: transform 0.2s ease, background 0.2s ease;
    animation: pulse 2.5s infinite;
    border: 2px solid var(--color-black);
  }
  .phone-fab:hover {
    background: var(--color-yellow-hover);
    color: var(--color-black);
    transform: scale(1.06);
  }

  @media (min-width: 768px) {
    .phone-fab {
      display: none;
    }
  }

  @keyframes pulse {
    0%, 100% { box-shadow: 0 6px 20px rgba(254, 196, 32, 0.5), 0 0 0 0 rgba(254, 196, 32, 0.5); }
    70% { box-shadow: 0 6px 20px rgba(254, 196, 32, 0.5), 0 0 0 16px rgba(254, 196, 32, 0); }
  }

  @media (prefers-reduced-motion: reduce) {
    .phone-fab { animation: none; }
  }
</style>
